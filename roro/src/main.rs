//! roro - Rust rewrite of Mistral Vibe CLI
//!
//! Main entry point for the CLI application.

use anyhow::Result;

fn main() -> Result<()> {
    println!("roro - Rust rewrite of Mistral Vibe CLI");
    Ok(())
}
